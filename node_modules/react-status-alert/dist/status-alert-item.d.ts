import { default as React } from 'react';
export type AlertType = 'success' | 'error' | 'info' | 'warning';
export interface AlertOptions {
    autoHide?: boolean;
    autoHideTime?: number;
    withIcon?: boolean;
    withCloseIcon?: boolean;
    color?: string;
    backgroundColor?: string;
    removeAllBeforeShow?: boolean;
}
export declare const defaultAlertOptions: AlertOptions;
export interface Alert {
    id: string;
    message: JSX.Element | string | object;
    type: AlertType;
    options: AlertOptions;
}
export interface StatusAlertItemProps {
    alert: Alert;
}
export declare const StatusAlertItem: React.FC<StatusAlertItemProps>;
